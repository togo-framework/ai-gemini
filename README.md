# ai-gemini

Google Gemini driver for the togo `ai` plugin (chat + embeddings).

```bash
togo install togo-framework/ai-gemini
```

Set `AI_DRIVER=gemini` and `GEMINI_API_KEY=...`. Default model `gemini-1.5-flash`; embeddings `text-embedding-004`. Token usage via `ai.Usage`.

MIT © ToGO
